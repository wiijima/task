<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task| %>
        <li>
            <p><%= link_to task.id, task_path(task) %> 
            :
            <%= task.content %></p>
        </li>
    <% end %>
</ul>

<%= link_to '新規タスクの投稿', new_task_path %>